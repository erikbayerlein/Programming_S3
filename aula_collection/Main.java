import java.util.*;

public class Main {
    public static void main (String[] args) {
        /* Set set = new HashSet();

        set.add("one");
        set.add("second");
        set.add("third");
        set.add(new Integer(4));
        set.add(new Float(5.0F));
        set.add("second"); // duplicate isn't added
        set.add(new Integer(4)); // duplicate isn't added
        System.out.println(set); */

        /* List list = new ArrayList();

        list.add("one");
        list.add("second");
        list.add("third");
        list.add(new Integer(4));
        list.add(new Float(5.0F));
        list.add("second"); // duplicate is added
        list.add(new Integer(4)); // duplicate is added
        System.out.println(list);

        Iterator elements = list.iterator();

        while (elements.hasNext()) {
            System.out.println(elements.next());
        } */

        /* Map mapa = new HashMap();

        mapa.put("um", "primeiro valor");
        mapa.put("dois", "segundo valor");
        mapa.put("tres", "terceiro valor");
        System.out.println("Valor recuperado: " + mapa.get("dois"));

        Set chaves = mapa.keySet();
        Collection valores = mapa.values();
    
        System.out.println("\n");

        System.out.println(chaves);
        System.out.println(valores); */

        // escreva uma frase na string abaixo
        String s = "eu amo amo java";
        Integer total, dif;
        String[] words = s.split(" ");
        total = words.length;

        List lista = new ArrayList<>();

        for (int i = 0; i < total; i++) {
            lista.add(words[i]);
        }
      
        Iterator elements = lista.iterator();

        



        int j;


        for (int i = 0; i < total; i++) {
            if (words[i] == )
            mapa.put(words[i], );
        }

        List list = new Vector();

        list.add(frase);

        System.out.println(list);

        Iterator elements = list.iterator();
        Integer total = 0;
        Integer palavras_distintas = 0;

        System.out.println(elements);


    
    }
}
